import { PagePlaceholder } from '@/shared/ui/PagePlaceholder';

export function CalendarRoute() {
  return (
    <PagePlaceholder
      eyebrow="Workspace"
      title="Calendar"
      description="Recurring Schedule Blocks, dated events, and date exceptions will be implemented after the migration preview is validated."
    />
  );
}
